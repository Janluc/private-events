<!DOCTYPE html>
<html>
  <head>
    <title>PrivateEvents</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>

  <body>
  <br>

    <% if current_user %>
      <span>Logged in as: <%= link_to "#{current_user.username}", current_user %></span>
      <span><%= link_to "Create Event!", new_event_url%></span>
      <% else %>
      <%= link_to "Login!", "/sign-in" %>
      <%= link_to "Sign up!", new_user_url %>
    <% end %>
    <span><%= link_to "View all Events!", events_url%></span>
    
    
    <%= yield %>

    <% unless logged_in? %>
      <%= link_to "Login!", "/sign-in" %>
      <%= link_to "Sign up!", new_user_url %>
      <% else %>
      <%= link_to "Create An Event!", new_event_url %>
    <% end %>
  </body>
</html>
